/**
 * 
 */
package com.whereistango.web.temp.controller;

import org.springframework.stereotype.Controller;

/**
 * @author resulav
 *
 */
@Controller
public class TmpMailController {

	// @RequestMapping(method = RequestMethod.GET)
	// public String getForm(ModelMap model) {
	//
	// model.addAttribute("message", "Hello Mahdi");
	// return "hello"; // assume hello.jsp
	// }
	//
	// @RequestMapping(params = {"submit"}, method = RequestMethod.GET)
	// public String printWelcome(ModelMap model, @RequestParam(value = "xx",
	// required = true) String mail, BindingResult result, SessionStatus status)
	// {
	//
	// ValidationUtils.rejectIfEmptyOrWhitespace(result, "mail",
	// "NotEmpty.userForm.email");
	//
	// if (result.hasErrors()) {
	// } else {
	// model.addAttribute("msg", "Thank you :) We took your mail address. We
	// will contact with you later with the mail : " + mail);
	// model.addAttribute("css", "success");
	// }
	//
	// return WitPage.HOME.direct();
	// }
}
